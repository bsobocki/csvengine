#include <gtest/gtest.h>

// Because of our CMake setup, we can include the header directly
#include <csvreader.hpp>

// Basic test to show how it works
TEST(GeneralTests, AlwaysPasses) {
    ASSERT_TRUE(true);
}
